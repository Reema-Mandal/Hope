CREATE A CUSTOM VPC WHERE U NEED TO CREATE TWO SUBNETS LIKE PRIVATE AND PUBLIC SUBNET IN THE PUBLIC SUBNET 1 WANTS TO HOST MY WEB SERVER
seach vpc in ec2
create vpc choose vpc only give name cidr=10.0.0/16
create vpc
 ec2 then internet gateway create new gateway give name and create 
back to internet gateway
select gateway 
actions
attach to vpc
select vpc name
attach internet gateway

click on subnet 
create two subnets 
only give name one public one private
put ipv4 as 10.0.0.0/24
create subnet
give name ( public and private with different zones)
create subnet twice

select the correct VPC name
ipv4 cidr = 10.0.0.0/16
ipv4 subnet = 10.0.1.0/24
create subnet

go to route table
create route table ( one for public one for private)
give name
select vpc
create route table
for the public one
edit route
add route 

add 0.0.0.0/0
add gateway internet gateway 
lgw full name select
save changes

go to subnet association 
edit subnet association
select public
save association

go to EC2
Launch 2 instances with same security group and key and  different region  in one instance auto assign public ip = enable
another one -- disable
create instance 
8gb gp2
launch instance

first connect the enabled (public)instance in terminal 
and also connect the private instance in same terminal
it wii appear as permission denied- then take ss 
##########################################################
{go to download 
open the key file in notepad copy it's content 
vim < within quotes the name and extension of the key > fcf.pem //example
inside the file paste the copied contents
come outside
chmod 400<within quotes key name with extension >} dont do from 53 to 58


goto EC2 
search vpc
click nat gateway
give name
select the subnet by name (public)
connectivity type = public
click on allocate elastic  IP
click on create nat gateway

GOTO ROUte tables 
select the route
click on route table id
edit route table
add route
0.o.00/0
select nat gateway
save changes

in the first instance (public)
goto security group 
add rules
ICMP
HTTP
All zero
Save
in the second instance
copy private IPV4
Paste in terminal
Ping google,com

